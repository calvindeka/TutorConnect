# Server
PORT=4131
HOST=0.0.0.0
ADVERTISED_IP=10.192.145.179
SESSION_SECRET=change-me-in-production

# Database (MariaDB / MySQL)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=tutorconnect

# Optional: enables AI tutor recommendations. If unset, /api/recommend uses a deterministic fallback.
OPENAI_API_KEY=
